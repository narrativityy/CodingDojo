// store the amount of likes a user has
var likeCount = 0;
// when called increase the users likeCount by 1
function increaseLikes() {
    // updating the likeCount variable by adding 1 to the previous count
    likeCount = likeCount + 1;
}